const { expect } = require("chai");

describe("Permit2Handler", function () {
  it("should transfer tokens based on a valid permit", async function () {
    // Simulate a valid signed permit and call permitTransfer
  });

  it("should revert if the permit is expired", async function () {
    // Simulate an expired permit and check for revert
  });
});
